{
  "$schema": "./node_modules/nx/schemas/nx-schema.json",
  "defaultBase": "main",
  "cacheDirectory": ".nx/cache",
  "namedInputs": {
    "sharedGlobals": [
      "{workspaceRoot}/nx.json",
      "{workspaceRoot}/package.json",
      "{workspaceRoot}/package-lock.json",
      "{workspaceRoot}/rust-toolchain.toml",
      "{workspaceRoot}/rustfmt.toml",
      "{workspaceRoot}/clippy.toml",
      "{workspaceRoot}/justfile"
    ],
    "default": ["{projectRoot}/**/*", "sharedGlobals"],
    "crateSource": [
      "{projectRoot}/**/*",
      "{workspaceRoot}/crates/**/*",
      "{workspaceRoot}/Cargo.toml",
      "{workspaceRoot}/Cargo.lock",
      "sharedGlobals"
    ],
    "crateAndSuiteInputs": [
      "crateSource",
      "{workspaceRoot}/docs/contract.md",
      "{workspaceRoot}/scripts/**/*",
      "{workspaceRoot}/npm/**/*",
      "{workspaceRoot}/.github/workflows/*"
    ]
  },
  "targetDefaults": {
    "bootstrap": { "cache": false },
    "format": { "cache": false },
    "format-check": { "cache": true, "inputs": ["crateSource"] },
    "lint": { "cache": true, "inputs": ["crateSource"] },
    "doc": { "cache": true, "inputs": ["crateSource"] },
    "test": { "cache": true, "inputs": ["crateAndSuiteInputs"] },
    "check": {
      "cache": false,
      "dependsOn": ["format-check", "lint", "test", "doc"]
    }
  }
}
