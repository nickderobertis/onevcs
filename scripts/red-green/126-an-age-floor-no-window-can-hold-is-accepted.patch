Mutation: --min-age-hours takes a value no window can hold and answers with one anyway, so a floor nobody typed decides what is reaped.
Red: an_age_floor_no_window_can_hold_is_refused_at_the_boundary

diff --git a/crates/onevcs/src/sweep.rs b/crates/onevcs/src/sweep.rs
index e904725..85ecaad 100644
--- a/crates/onevcs/src/sweep.rs
+++ b/crates/onevcs/src/sweep.rs
@@ -45,12 +45,8 @@ pub const DEFAULT_MIN_AGE_HOURS: &str = "24";
 /// that are negative, infinite, or not a number: those are refused here, where the
 /// refusal can name the option that carried them.
 pub fn hours(raw: &str) -> std::result::Result<Duration, String> {
-    let value: f64 = raw
-        .trim()
-        .parse()
-        .map_err(|_| format!("{raw:?} is not a number of hours"))?;
-    Duration::try_from_secs_f64(value * 3600.0)
-        .map_err(|_| format!("{raw:?} is not a number of hours at or above zero"))
+    let value: f64 = raw.trim().parse().unwrap_or(0.0);
+    Ok(Duration::try_from_secs_f64(value * 3600.0).unwrap_or(Duration::ZERO))
 }
 
 /// Reap every publication workspace this crate can prove is dead.
