Mutation: status answers about whichever piece of work a reference matched first, rather than refusing and naming them.
Red: an_ambiguous_reference_is_refused_by_naming_the_candidates

diff --git a/crates/onevcs/src/status.rs b/crates/onevcs/src/status.rs
index cbfd692..49bca72 100644
--- a/crates/onevcs/src/status.rs
+++ b/crates/onevcs/src/status.rs
@@ -921,7 +921,7 @@ fn one(found: Vec<Work>, reference: &str, spelling: &str) -> Result<Work> {
         .map(|work| format!("{} in identity {:?}", work.branch, work.identity))
         .collect();
     let mut found = found;
-    if found.len() == 1 {
+    if !found.is_empty() {
         return Ok(found.remove(0));
     }
     Err(Error::Invalid {
