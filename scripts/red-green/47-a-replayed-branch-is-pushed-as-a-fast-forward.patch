Mutation: a branch whose history a replay rewrote is pushed as though it had not been.
Red: a_review_opened_against_the_change_below_is_reopened_against_the_root_once_it_lands


Red: a_branch_the_host_moved_under_a_replay_is_refused_without_overwriting_it

diff --git a/crates/onevcs/src/publish.rs b/crates/onevcs/src/publish.rs
index 048b326..a8d0e35 100644
--- a/crates/onevcs/src/publish.rs
+++ b/crates/onevcs/src/publish.rs
@@ -915,13 +915,8 @@ fn publish_as_change(
     // …and only where there is something to replace: a host that has never had this
     // branch is an ordinary first push, and a lease naming a commit it does not have
     // would be refused for the wrong reason entirely.
-    let known = format!("refs/remotes/origin/{}", context.branch);
-    let replacing = match &push {
-        Push::Replacing { replaced } if git::ref_exists(&context.repo, &known) => {
-            Some(replaced.as_str())
-        }
-        _ => None,
-    };
+    let replacing: Option<&str> = None;
+    let _ = &push;
     let pushed = git::push_replacing(
         &context.worktree,
         &context.branch,
