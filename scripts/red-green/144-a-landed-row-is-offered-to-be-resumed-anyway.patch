Mutation: a row whose work reached the base carries the argv that publishes it again, and prints it as ready to resume.
Red: a_landing_this_host_kept_no_record_of_is_read_off_the_trailer_it_left_on_the_base
Red: a_branch_this_host_landed_with_no_change_request_reads_as_landed_by_the_landing_it_recorded

diff --git a/crates/onevcs/src/app.rs b/crates/onevcs/src/app.rs
index 27b2594..780de4a 100644
--- a/crates/onevcs/src/app.rs
+++ b/crates/onevcs/src/app.rs
@@ -551,17 +551,6 @@ fn recoverable(args: &RecoverableArgs, providers: &Providers<'_>) -> Result<u8>
         // no such line at all — running it would re-open a change request for work the
         // base already carries — and one nothing can decide about is told what to look
         // at first.
-        if let Landed::Yes { evidence } = &row.landed {
-            println!(
-                "    Landed: {tier} ({commit}) says this branch's work reached {base}. Nothing \
-                 to resume — publishing it again would re-open a change request for work \
-                 {base} already carries",
-                tier = row.landed.tier(),
-                commit = evidence.commit(),
-                base = row.branch.base,
-            );
-            continue;
-        }
         if row.landed == Landed::Unknown {
             println!(
                 "    Not decided: nothing records that this branch's work reached {base} — no \
diff --git a/crates/onevcs/src/vcs.rs b/crates/onevcs/src/vcs.rs
index 98eda2c..2a6591e 100644
--- a/crates/onevcs/src/vcs.rs
+++ b/crates/onevcs/src/vcs.rs
@@ -513,16 +513,13 @@ fn preserved_row(
     // surface in docs/inferred-surface.md fixes; renaming that field is a
     // break of the published surface, and a local that disagreed with it
     // would be the drift. Which verb the argv holds is the two lines above.
-    let recover_command = match verdict.is_landed() {
-        true => Vec::new(),
-        false => vec![
-            "onevcs".to_owned(),
-            verb.to_owned(),
-            branch.to_owned(),
-            "--repo".to_owned(),
-            publication.display().to_string(),
-        ],
-    };
+    let recover_command = vec![
+        "onevcs".to_owned(),
+        verb.to_owned(),
+        branch.to_owned(),
+        "--repo".to_owned(),
+        publication.display().to_string(),
+    ];
     Ok((
         git::committed_at(repo, branch),
         Recoverable {
