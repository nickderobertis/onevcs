Mutation: the opening of a continued session stops saying so, so a follower reading the stream cannot tell a session that found the work from one that started over.
Red: a_branch_pin_naming_work_that_already_exists_continues_it_rather_than_cutting_fresh
Red: a_continued_branch_opens_at_whichever_copy_carries_the_other

diff --git a/crates/onevcs/src/workspace.rs b/crates/onevcs/src/workspace.rs
index 2567306..8cc44c5 100644
--- a/crates/onevcs/src/workspace.rs
+++ b/crates/onevcs/src/workspace.rs
@@ -667,10 +667,7 @@ pub fn open(registry: &Registry, request: &SessionRequest) -> Result<(Record, St
         owner_started: process_started(std::process::id()),
     };
     save(&record)?;
-    let reuse = match continued {
-        Some(_) => Reuse::Continued,
-        None => Reuse::Cut,
-    };
+    let reuse = Reuse::Cut;
     stream.emit(EventKind::SessionOpened, opened(&record, reuse));
     drop(lease);
     Ok((record, stream))
