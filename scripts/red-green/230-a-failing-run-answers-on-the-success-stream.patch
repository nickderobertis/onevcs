Mutation: a run with findings hands the judge's report back on stdout, where a caller reads this tier's answer, instead of as the diagnostic it is.
Red: findings_fail_the_tier_and_are_never_replayed

diff --git a/scripts/llmlint-diff.sh b/scripts/llmlint-diff.sh
index 19e450c..1cb2eb9 100755
--- a/scripts/llmlint-diff.sh
+++ b/scripts/llmlint-diff.sh
@@ -103,7 +103,7 @@ LLMLINT_DIFF_BASE_SHA="$base_sha" ONEVCS_NX_SHOW_OUTPUT=1 \
 # the task — so all of it goes to stderr. Only the one line a clean run prints is an
 # answer, and that is the only thing this tier ever puts on stdout.
 if [ "$status" -ne 0 ]; then
-  cat "$verdict" >&2
+  cat "$verdict"
   cat "$diagnostics" >&2
 fi
 
