Mutation: a seeded body about a change request nobody opened stops being refused.
Red: a_seeded_document_holding_a_session_nothing_could_act_on_is_refused

diff --git a/crates/onevcs-testing/src/state.rs b/crates/onevcs-testing/src/state.rs
index e5f02d1..3e7fe25 100644
--- a/crates/onevcs-testing/src/state.rs
+++ b/crates/onevcs-testing/src/state.rs
@@ -541,9 +541,7 @@ impl Checked for HostState {
             titled(title)?;
         }
         for id in self.bodies.keys() {
-            // Nothing is refused about the body itself: a host places no shape on
-            // prose, and a change request opened with an empty one is a scenario.
-            opened_change(self, id, "a body")?;
+            let _ = id;
         }
         Ok(())
     }
