Mutation: a listing that did not arrive whole is answered as content the base carries.
Red: an_unreadable_listing_and_a_root_that_moved_on_leaves_the_stack_where_it_is

diff --git a/crates/onevcs/src/git.rs b/crates/onevcs/src/git.rs
index 7cf18c0..7c7a63c 100644
--- a/crates/onevcs/src/git.rs
+++ b/crates/onevcs/src/git.rs
@@ -804,7 +804,7 @@ pub fn known_to_carry_changes(cwd: &Path, base: &str, fork: &str, commit: &str)
         // Which leaves the same question asked without paths: a base carrying this
         // commit's whole tree carries its changes too, and that is the one answer
         // that cannot be wrong about a path nobody here could name.
-        return Ok(!trees_differ(cwd, base, commit)?);
+        return Ok(true);
     }
     if touched.is_empty() {
         // The commit changed nothing since the fork, and a base built on that fork
