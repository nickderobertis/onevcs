Mutation: the comparison of content answers no rather than unknown, so a landing nothing recorded reads as work nobody published.
Red: a_branch_landed_with_no_change_request_and_not_through_this_host_reads_as_unknown

diff --git a/crates/onevcs/src/landed.rs b/crates/onevcs/src/landed.rs
index 446e9ad..1db71c5 100644
--- a/crates/onevcs/src/landed.rs
+++ b/crates/onevcs/src/landed.rs
@@ -216,7 +216,7 @@ pub(crate) fn decide(
     // base beside it does not change the answer — which is the failure that put this
     // module here.
     if git::known_to_carry_changes(repo, compared, &fork, branch)? {
-        return Ok(Landed::Unknown);
+        return Ok(Landed::No);
     }
     // The base does not carry what this branch changed, and that is *usually* work
     // nobody published. It is not, when the base's own history has already taken a
