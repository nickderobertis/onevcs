Mutation: the harness stops holding this branch's new tests to a round that breaks them.
Red: a_test_added_since_the_base_that_no_mutation_breaks_fails_the_run

diff --git a/scripts/red-green.sh b/scripts/red-green.sh
index c33cfee..06d8b92 100755
--- a/scripts/red-green.sh
+++ b/scripts/red-green.sh
@@ -290,12 +290,6 @@ while read -r test; do
   [ -n "$test" ] || continue
   grep -qx "$test" <<<"$covered" || missing+="  $test"$'\n'
 done <<<"$added"
-if [ -n "$missing" ]; then
-  echo "red-green: these tests are new since $base and no patch turns them red:" >&2
-  printf '%s' "$missing" >&2
-  echo "ACTION: add a patch under scripts/red-green/ that removes the behaviour each one is about, or say in its own body why it cannot be broken" >&2
-  exit 1
-fi
 
 # …and green, unmutated, which is the other half of the claim.
 green=0
