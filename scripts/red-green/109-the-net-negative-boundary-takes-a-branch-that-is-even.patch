Mutation: the net-negative boundary stops being strict, so a branch that adds exactly as much as it removes is a mark this type will hold.
Red: what_the_net_negative_count_does_not_count_leaves_a_branch_unmarked
Red: the_reported_shapes_serialize_the_way_a_json_consumer_reads_them

diff --git a/crates/onevcs/src/session.rs b/crates/onevcs/src/session.rs
index bdb3a3e..7b7f953 100644
--- a/crates/onevcs/src/session.rs
+++ b/crates/onevcs/src/session.rs
@@ -289,7 +289,7 @@ pub struct NetNegative(LineChange);
 impl NetNegative {
     /// The count, when it is one a net-negative branch could have.
     pub fn new(lines: LineChange) -> Option<Self> {
-        (lines.removed > lines.added).then_some(Self(lines))
+        (lines.removed >= lines.added).then_some(Self(lines))
     }
 
     /// Lines the branch adds.
