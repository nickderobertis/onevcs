Mutation: the net-negative boundary stops being strict, so a branch that adds exactly as much as it removes is marked as one that strips work.
Red: what_the_net_negative_count_does_not_count_leaves_a_branch_unmarked

diff --git a/crates/onevcs/src/vcs.rs b/crates/onevcs/src/vcs.rs
index b1e3065..807bab2 100644
--- a/crates/onevcs/src/vcs.rs
+++ b/crates/onevcs/src/vcs.rs
@@ -443,7 +443,7 @@ fn net_negative(repo: &Path, compared: &str, branch: &str) -> Result<Option<Line
         return Ok(None);
     };
     let counted = git::line_change(repo, &fork, branch)?;
-    Ok((counted.removed > counted.added).then_some(LineChange {
+    Ok((counted.removed >= counted.added).then_some(LineChange {
         added: counted.added,
         removed: counted.removed,
     }))
