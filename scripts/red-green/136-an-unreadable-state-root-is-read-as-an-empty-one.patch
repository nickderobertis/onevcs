Mutation: a state root that is there and unreadable is read as one nobody has published from, so a sweep that could not run answers as one that found nothing to do.
Red: a_state_root_this_host_cannot_read_is_a_sweep_that_could_not_run

diff --git a/crates/onevcs/src/sweep.rs b/crates/onevcs/src/sweep.rs
index 79dc591..0db884b 100644
--- a/crates/onevcs/src/sweep.rs
+++ b/crates/onevcs/src/sweep.rs
@@ -109,8 +109,7 @@ pub fn run(dry_run: bool, min_age: Duration) -> Result<Report> {
                 });
             }
         }
-        Err(e) if e.kind() == std::io::ErrorKind::NotFound => {}
-        Err(e) => return Err(error::at("read the workspaces under", &root)(e)),
+        Err(_) => {}
     }
     report
         .skipped
