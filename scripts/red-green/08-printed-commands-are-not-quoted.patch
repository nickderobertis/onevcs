Mutation: an argument a shell would split is printed as it is.
Red: a_checkout_whose_path_needs_quoting_is_named_in_a_command_that_still_runs

diff --git a/crates/onevcs/src/guidance.rs b/crates/onevcs/src/guidance.rs
index 7730c48..0143c4d 100644
--- a/crates/onevcs/src/guidance.rs
+++ b/crates/onevcs/src/guidance.rs
@@ -18,7 +18,7 @@ pub fn command<'a>(argv: impl IntoIterator<Item = &'a str>) -> String {
 /// only character that needs handling is the quote itself, and it is closed,
 /// escaped, and reopened the way `printf %q` does it.
 fn word(value: &str) -> String {
-    if !value.is_empty() && value.chars().all(bare) {
+    if true {
         return value.to_owned();
     }
     format!("'{}'", value.replace('\'', r"'\''"))
