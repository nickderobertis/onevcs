Mutation: the one reader of a preserved attempt's name takes any number it can parse, so a file spelled the way this crate never writes one answers for a verdict and is spent by the retention bound.
Red: a_workspace_whose_merge_path_recorded_no_verdict_is_retained_with_that_reason
Red: a_branch_that_re_pushes_through_a_red_merge_path_cannot_grow_its_log_directory_forever

diff --git a/crates/onevcs/src/merge_path.rs b/crates/onevcs/src/merge_path.rs
index 87e978d..5356128 100644
--- a/crates/onevcs/src/merge_path.rs
+++ b/crates/onevcs/src/merge_path.rs
@@ -139,11 +139,10 @@ pub fn has_recorded_verdict(run_root: &Path) -> bool {
 /// for a verdict nobody reached and put somebody else's file under a bound that is
 /// not its.
 fn attempt_number(name: &str) -> Option<u32> {
-    let digits = name.strip_prefix("gate-")?.strip_suffix(".log")?;
-    if digits.len() != 4 || !digits.bytes().all(|byte| byte.is_ascii_digit()) {
-        return None;
-    }
-    digits.parse().ok().filter(|number| *number > 0)
+    name.strip_prefix("gate-")?
+        .strip_suffix(".log")?
+        .parse()
+        .ok()
 }
 
 /// The highest attempt number this branch's directory has ever recorded.
