Mutation: the status report stops declaring its schema version, so a consumer reads a shape it inferred from the keys it could find.
Red: the_status_report_is_the_versioned_object_its_goldens_record

diff --git a/crates/onevcs/src/status.rs b/crates/onevcs/src/status.rs
index 7c43fc6..05086fa 100644
--- a/crates/onevcs/src/status.rs
+++ b/crates/onevcs/src/status.rs
@@ -59,6 +59,7 @@ pub const REPORT_VERSION: u32 = 1;
 pub struct Report {
     /// The schema version this object is written at, so a consumer reads a shape it
     /// was told rather than one it guessed.
+    #[serde(skip)]
     pub version: u32,
     /// The reference as it was asked for, and how it was read.
     #[serde(rename = "ref")]
