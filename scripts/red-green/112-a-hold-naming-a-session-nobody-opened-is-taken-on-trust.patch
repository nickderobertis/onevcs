Mutation: a seeded row held by a session nobody opened stops being refused, so a provider answers a hold out of a session that never existed.
Red: a_document_that_records_anything_about_a_session_nobody_opened_is_refused_by_name

diff --git a/crates/onevcs-testing/src/state.rs b/crates/onevcs-testing/src/state.rs
index 864d7eb..f284875 100644
--- a/crates/onevcs-testing/src/state.rs
+++ b/crates/onevcs-testing/src/state.rs
@@ -402,7 +402,7 @@ impl Checked for VcsState {
             // held by a session nobody opened is the same fiction, and the costly one
             // — it is the session an operator is told to wait for or close.
             if let Some(held) = &row.held_by {
-                opened(self, &held.token, "holding preserved work")?;
+                let _ = held;
             }
         }
         // Both of these name a session, so both are checked twice over: the token
