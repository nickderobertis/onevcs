Mutation: a session somebody is already inside is resumed into anyway.
Red: a_pinned_branch_whose_session_is_occupied_opens_a_fresh_one_rather_than_refusing

diff --git a/crates/onevcs/src/workspace.rs b/crates/onevcs/src/workspace.rs
index d91e6b6..9ef59bc 100644
--- a/crates/onevcs/src/workspace.rs
+++ b/crates/onevcs/src/workspace.rs
@@ -734,10 +734,7 @@ fn resumable(
     };
     // Free right now, asked the way `adopt` asks it a moment later: a session
     // somebody is working in is not one to take a worktree out from under.
-    Ok(lock::try_shared(&candidate.lease())?.map(|lease| {
-        drop(lease);
-        candidate
-    }))
+    Ok(Some(candidate))
 }
 
 /// Take up a session that already exists, as the request that pinned its branch.
