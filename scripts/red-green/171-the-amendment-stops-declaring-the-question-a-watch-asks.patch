Mutation: the amendment stops declaring the method a watching publication asks its host.
Red: the_amendment_declares_the_question_a_watched_publication_asks_its_host

diff --git a/docs/contract.md b/docs/contract.md
index 937d163..b11cba2 100644
--- a/docs/contract.md
+++ b/docs/contract.md
@@ -448,7 +448,7 @@ seventh:
 
 ```rust
 pub trait RemoteHost {                       // the six above, unchanged, plus:
-    fn merged_at(&self, cr: &ChangeRequest) -> Result<Option<Sha>>;
+    fn merged_at(&self, cr: &ChangeRequest) -> Result<Option<Sha>>;  // spelling to be settled
 }
 ```
 
