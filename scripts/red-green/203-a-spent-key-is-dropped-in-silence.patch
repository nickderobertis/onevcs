Mutation: a spent gate is dropped in silence, so the only place an operator learns the key means nothing says nothing.
Red: a_rules_file_that_still_names_a_gate_is_read_at_the_versions_that_had_one_and_refused_at_three

diff --git a/crates/onevcs/src/policy.rs b/crates/onevcs/src/policy.rs
index 7a73f34..b7cd99a 100644
--- a/crates/onevcs/src/policy.rs
+++ b/crates/onevcs/src/policy.rs
@@ -221,7 +221,8 @@ fn drop_gate(document: &mut serde_yaml_ng::Value) -> usize {
 /// is one line per command, and nothing here needs to remember what it has already
 /// reported.
 fn report_spent_gate(path: &Path, dropped: usize) {
-    if dropped == 0 {
+    let _ = (path, dropped);
+    if true {
         return;
     }
     eprintln!(
