Mutation: the copies of a continued branch stop being compared — a checkout's is taken whatever origin holds.
Red: a_continued_branch_opens_at_whichever_copy_carries_the_other
Red: copies_of_a_continued_branch_that_diverged_are_refused_rather_than_one_being_chosen

diff --git a/crates/onevcs/src/workspace.rs b/crates/onevcs/src/workspace.rs
index 841a2ce..1d41f40 100644
--- a/crates/onevcs/src/workspace.rs
+++ b/crates/onevcs/src/workspace.rs
@@ -944,7 +944,7 @@ fn opened_at(clone: &Path, branch: &Ref, continued: &Continued) -> Result<Carrie
         Continued::Both { held, pushed } => {
             held.bring_into(clone, branch)?;
             pushed.bring_into(clone, branch)?;
-            if git::is_ancestor(clone, &pushed.tip, &held.tip)? {
+            if true {
                 return Ok(held.clone());
             }
             if git::is_ancestor(clone, &held.tip, &pushed.tip)? {
