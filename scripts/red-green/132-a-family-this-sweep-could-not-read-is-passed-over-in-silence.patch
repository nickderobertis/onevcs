Mutation: a family of run roots this sweep could not even list is left out of the report, so a directory full of workspaces reads as one holding none.
Red: what_this_host_may_not_read_or_remove_is_reported_rather_than_failing_the_sweep

diff --git a/crates/onevcs/src/sweep.rs b/crates/onevcs/src/sweep.rs
index e0754cb..765e9f3 100644
--- a/crates/onevcs/src/sweep.rs
+++ b/crates/onevcs/src/sweep.rs
@@ -192,13 +192,7 @@ fn family(report: &mut Report, verb: Verb, min_age: Duration) -> Result<()> {
             });
             return Ok(());
         }
-        Err(e) => {
-            report.skipped.push(Skipped {
-                path: directory,
-                reason: format!("cannot read this family of run roots: {e}"),
-            });
-            return Ok(());
-        }
+        Err(_) => return Ok(()),
     };
     let mut run_roots: Vec<PathBuf> = Vec::new();
     let mut unreadable: Vec<String> = Vec::new();
