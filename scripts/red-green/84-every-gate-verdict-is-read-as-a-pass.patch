Mutation: status stops reading what a gate-verdict event actually said, so a refusal and a word this build does not know both report as a pass.
Red: the_last_gate_verdict_recorded_for_the_work_is_what_the_report_names

diff --git a/crates/onevcs/src/status.rs b/crates/onevcs/src/status.rs
index 1ac616c..f992f82 100644
--- a/crates/onevcs/src/status.rs
+++ b/crates/onevcs/src/status.rs
@@ -890,11 +890,7 @@ fn read_stream(directory: &Path, token: &str, notes: &mut Vec<String>) -> Record
                 record.gate = Some(Stamped {
                     at,
                     value: GateReport {
-                        verdict: match field("verdict").as_deref() {
-                            Some("pass") => Verdict::Pass,
-                            Some("fail") => Verdict::Fail,
-                            _ => Verdict::Unrecorded,
-                        },
+                        verdict: Verdict::Pass,
                         command: field("command")
                             .unwrap_or_else(|| "a command the event did not name".to_owned()),
                         log: field("preserved_log").map(PathBuf::from),
