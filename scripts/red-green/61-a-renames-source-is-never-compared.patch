Mutation: a rename is compared under the name git reports it as, and not under the one it left.
Red: a_stack_that_renamed_a_file_the_root_still_has_keeps_targeting_the_stack

diff --git a/crates/onevcs/src/git.rs b/crates/onevcs/src/git.rs
index 809d48a..7411420 100644
--- a/crates/onevcs/src/git.rs
+++ b/crates/onevcs/src/git.rs
@@ -738,7 +738,7 @@ pub fn merge_base(cwd: &Path, first: &str, second: &str) -> Result<Option<String
 /// detection for the same reason the listing does.
 fn counted_files(cwd: &Path, from: &str, to: &str) -> Result<usize> {
     let summary = checked(
-        &["diff", "--shortstat", "--no-renames", from, to],
+        &["diff", "--shortstat", from, to],
         Some(cwd),
     )?
     .trimmed();
@@ -775,7 +775,7 @@ pub fn carries_changes(cwd: &Path, base: &str, fork: &str, commit: &str) -> Resu
     // still on the base — which is the half of a rename that says the change below has
     // *not* landed. Without detection both paths are listed and both are compared.
     let listed = checked(
-        &["diff", "--name-only", "--no-renames", "-z", fork, commit],
+        &["diff", "--name-only", "-z", fork, commit],
         Some(cwd),
     )?;
     // Pathspecs, not names: a path is a repository's own content and one beginning
