Mutation: the harness stops running the tests with the behaviour back in place.
Red: a_test_that_is_red_with_the_behaviour_in_place_fails_the_run

diff --git a/scripts/red-green.sh b/scripts/red-green.sh
index fc122d7..b5c84fd 100755
--- a/scripts/red-green.sh
+++ b/scripts/red-green.sh
@@ -260,11 +260,6 @@ while read -r test; do
   output="$(run_one "$test" || true)"
   note "=== $test (unmutated)
 $output"
-  if ! grep -q "1 test run: 1 passed" <<<"$output"; then
-    echo "red-green: $test does not pass unmutated" >&2
-    echo "ACTION: read $log — the tree is green only if every test observed red above also passes with the behaviour in place" >&2
-    exit 1
-  fi
   green=$((green + 1))
 done < <(sort -u <<<"$covered")
 
