Mutation: status stops reading what a push event actually said, so a refusal and a word this build does not know both report as a pass.
Red: the_last_merge_path_verdict_recorded_for_the_work_is_what_the_report_names

diff --git a/crates/onevcs/src/status.rs b/crates/onevcs/src/status.rs
index 3b5bbda..c7018b4 100644
--- a/crates/onevcs/src/status.rs
+++ b/crates/onevcs/src/status.rs
@@ -1217,11 +1217,7 @@ fn read_stream(directory: &Path, token: &str, notes: &mut Vec<String>) -> Record
                 record.merge_path = Some(Stamped {
                     at,
                     value: MergePathReport {
-                        verdict: match event.payload.get("accepted").and_then(Value::as_bool) {
-                            Some(true) => Verdict::Pass,
-                            Some(false) => Verdict::Fail,
-                            None => Verdict::Unrecorded,
-                        },
+                        verdict: Verdict::Pass,
                         log: field("preserved_log").map(PathBuf::from),
                         recorded_by: token.to_owned(),
                     },
