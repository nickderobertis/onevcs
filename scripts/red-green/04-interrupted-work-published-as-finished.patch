Mutation: publish-branch stops refusing a branch that carries an unattested incomplete marker.
Red: publishing_a_branch_refuses_interrupted_work_and_names_the_verb_that_lands_it

diff --git a/crates/onevcs/src/publish_branch.rs b/crates/onevcs/src/publish_branch.rs
index 3a7a24e..1e0a885 100644
--- a/crates/onevcs/src/publish_branch.rs
+++ b/crates/onevcs/src/publish_branch.rs
@@ -35,7 +35,7 @@ pub fn run(
     let landing = branch::prepare(registry, Verb::PublishBranch, repo, branch, policy)?;
 
     let unattested = landing.unattested()?;
-    if !unattested.is_empty() {
+    if false && !unattested.is_empty() {
         return Err(Error::Invalid {
             reason: format!(
                 "branch {branch:?} carries incomplete provenance ({} unattested marker(s)): a \
