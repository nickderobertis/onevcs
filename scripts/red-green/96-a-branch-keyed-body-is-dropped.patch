Mutation: the branch-keyed verbs stop carrying the caller's body into the publication.
Red: a_complete_branch_opens_its_change_request_with_the_body_the_caller_drafted
Red: a_recovery_opens_its_change_request_with_the_body_the_caller_drafted

diff --git a/crates/onevcs/src/branch.rs b/crates/onevcs/src/branch.rs
index 04602a5..6271ee1 100644
--- a/crates/onevcs/src/branch.rs
+++ b/crates/onevcs/src/branch.rs
@@ -487,10 +487,10 @@ impl Landing {
             },
             run_root: self.run_root.clone(),
             title,
-            // Both branch-keyed verbs carry a caller's body now: a branch is landed
-            // by whichever verb its provenance belongs to, and which one that was is
-            // no reason for the change request to describe itself differently.
-            body,
+            body: {
+                let _ = body;
+                None
+            },
             trailers: Vec::new(),
             provenance: self.trailers.clone(),
             hosting,
