Mutation: the sync after the queue replays from a boundary the branch no longer carries.
Red: a_root_that_advances_after_the_gate_is_resynced_without_the_stack_returning

diff --git a/crates/onevcs/src/publish.rs b/crates/onevcs/src/publish.rs
index 23865c3..9f5a9ae 100644
--- a/crates/onevcs/src/publish.rs
+++ b/crates/onevcs/src/publish.rs
@@ -546,7 +546,9 @@ pub fn run(context: &Context<'_>, stream: &mut Stream) -> Result<PublishOutcome>
     verify(context, stream, &environment)?;
 
     match context.effective {
-        MergePolicy::LocalDirect => publish_locally(context, stream, &compared, &environment),
+        MergePolicy::LocalDirect => {
+            publish_locally(context, stream, &compared, &environment, replay.as_deref())
+        }
         _ => publish_as_change(context, stream, &subject, &trailers, &environment),
     }
 }
@@ -795,6 +797,7 @@ fn publish_locally(
     stream: &mut Stream,
     compared: &str,
     environment: &[(String, String)],
+    replay: Option<&str>,
 ) -> Result<PublishOutcome> {
     let publication = &context.resolution.publication;
     require_publication_checkout_ready(publication, context.target.base())?;
@@ -828,7 +831,7 @@ fn publish_locally(
             git::fetch(&context.repo, "origin")?;
         }
         if git::tip(&context.repo, compared) != judged {
-            sync(context, stream, compared, None)?;
+            sync(context, stream, compared, replay)?;
             verify(context, stream, environment)?;
         }
         let (subject, trailers) = describe(context, compared)?;
