Mutation: a rule and a policy stop refusing a key nobody declared, so the gate no schema has any more is read back in silence.
Red: a_rules_file_that_still_names_a_gate_is_not_a_shape_this_type_reads
Red: a_malformed_rules_file_is_rejected_at_the_boundary

diff --git a/crates/onevcs/src/rules.rs b/crates/onevcs/src/rules.rs
index 98f7dc9..acbcb4c 100644
--- a/crates/onevcs/src/rules.rs
+++ b/crates/onevcs/src/rules.rs
@@ -102,7 +102,6 @@ impl std::fmt::Display for TrailerPrefix {
 
 /// One rule: what it matches, and which parts of the policy it sets.
 #[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
-#[serde(deny_unknown_fields)]
 pub struct Rule {
     /// What this rule applies to.
     #[serde(rename = "match")]
@@ -136,7 +135,6 @@ pub struct RuleMatch {
 
 /// A complete policy: every field a rule may set, all of them decided.
 #[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
-#[serde(deny_unknown_fields)]
 pub struct Policy {
     /// How a change is published.
     pub publication: MergePolicy,
