Mutation: the age floor stops bounding what a sweep considers, so a workspace written moments ago is reaped by a sweep that was told to leave a day's work alone.
Red: the_age_floor_bounds_what_a_sweep_considers

diff --git a/crates/onevcs/src/sweep.rs b/crates/onevcs/src/sweep.rs
index c828fad..8b84259 100644
--- a/crates/onevcs/src/sweep.rs
+++ b/crates/onevcs/src/sweep.rs
@@ -349,12 +349,6 @@ fn judge(run_root: &Path, min_age: Duration) -> Result<Verdict> {
     let age = SystemTime::now()
         .duration_since(last_written(run_root))
         .unwrap_or(Duration::ZERO);
-    if age < min_age {
-        return Ok(Verdict::Retain(Kept::Fresh {
-            age,
-            floor: min_age,
-        }));
-    }
     // Last, because it is the only question whose answer is about this *host* rather
     // than about the workspace: everything above has already decided the directory is
     // dead, and this decides whether reaping it is ours to do.
