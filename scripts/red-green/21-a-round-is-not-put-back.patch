Mutation: the harness stops reverting a round once it is over.
Red: a_round_is_recorded_and_the_tree_is_left_as_it_was_found

diff --git a/scripts/red-green.sh b/scripts/red-green.sh
index 760bf96..e006d63 100755
--- a/scripts/red-green.sh
+++ b/scripts/red-green.sh
@@ -415,11 +415,7 @@ restore() {
     return 1
   fi
 }
-# Both, because one `trap ... EXIT` replaces the other: the lock taken above is
-# still held, and a run that put the tree back but kept the lock would turn away
-# every run after it.
-trap 'restore; release_lock' EXIT
-
+trap release_lock EXIT
 for patch in "${patches[@]}"; do
   name="$(basename "$patch" .patch)"
   mutation="$(sed -n 's/^Mutation:[[:space:]]*//p' "$patch")"
@@ -456,7 +452,6 @@ $output"
     transcript+="- RED \`$test\` — $(failure_line "$output")"$'\n'
   done
   transcript+=$'\n'
-  restore
 done
 
 # Every test this branch adds has to be in some patch's Red set: one that is not is
