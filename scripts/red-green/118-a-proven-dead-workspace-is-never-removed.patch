Mutation: a sweep reports what it reclaimed and removes none of it, which is the state before this verb: the directories are named and every one of them is still there.
Red: a_finished_publication_workspace_older_than_the_age_floor_is_reclaimed
Red: a_recovery_workspace_is_reaped_by_the_same_verb_as_a_publication
Red: a_dry_run_reports_what_it_would_reclaim_and_removes_nothing
Red: the_age_floor_bounds_what_a_sweep_considers

diff --git a/crates/onevcs/src/sweep.rs b/crates/onevcs/src/sweep.rs
index 1fa5a19..3a6e2e1 100644
--- a/crates/onevcs/src/sweep.rs
+++ b/crates/onevcs/src/sweep.rs
@@ -600,11 +600,6 @@ fn reclaim(report: &mut Report, run_root: PathBuf, lease: lock::Guard) -> Result
     // llmlint: ignore-block[changed_behavior_has_e2e] the shapes an operator meets — a
     // family this user may not write to, content it may not unlink — are decided above
     // and are journeys; what is left is the root changing between question and act.
-    if let Err(e) = std::fs::remove_dir_all(&run_root) {
-        return Err(error::at("remove the reclaimable workspace at", &run_root)(
-            e,
-        ));
-    }
     // llmlint: ignore-end[changed_behavior_has_e2e]
     report.reclaimed.push(Reclaimed {
         path: run_root,
