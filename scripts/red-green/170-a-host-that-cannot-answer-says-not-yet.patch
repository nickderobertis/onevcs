Mutation: a host that was never taught where a change landed answers "not yet" instead of refusing, so a publication watches to its bound and blames checks that were never the reason.
Red: the_amendment_declares_the_question_a_watched_publication_asks_its_host

diff --git a/crates/onevcs/src/host.rs b/crates/onevcs/src/host.rs
index caa29b0..66afac8 100644
--- a/crates/onevcs/src/host.rs
+++ b/crates/onevcs/src/host.rs
@@ -141,9 +141,7 @@ pub trait RemoteHost {
     /// way would watch until its bound and then report checks that were never the
     /// reason.
     fn merged_at(&self, _cr: &ChangeRequest) -> Result<Option<Sha>> {
-        Err(Error::NotImplemented {
-            operation: "RemoteHost::merged_at",
-        })
+        Ok(None)
     }
 }
 
