Mutation: --dry-run stops being a rehearsal: the run that was asked to report what it would do does it.
Red: a_dry_run_reports_what_it_would_reclaim_and_removes_nothing

diff --git a/crates/onevcs/src/sweep.rs b/crates/onevcs/src/sweep.rs
index c828fad..d964ef1 100644
--- a/crates/onevcs/src/sweep.rs
+++ b/crates/onevcs/src/sweep.rs
@@ -370,13 +370,6 @@ fn judge(run_root: &Path, min_age: Duration) -> Result<Verdict> {
 /// can take the run root up between the proof and the act.
 fn reclaim(report: &mut Report, run_root: PathBuf, lease: lock::Guard) -> Result<()> {
     let bytes = size_of(&run_root);
-    if report.dry_run {
-        report.reclaimed.push(Reclaimed {
-            path: run_root,
-            bytes,
-        });
-        return Ok(());
-    }
     // llmlint: ignore-block[changed_behavior_has_e2e] uncovered: a removal that fails
     // after `can_remove` asked every directory it would touch. Nothing this crate
     // exposes can produce one — the two shapes an operator meets, a family this user
