Mutation: a publishing push records what it wrote only where it was refused, and never on the event, so what a green run's merge path said is thrown away.
Red: a_push_a_hook_refuses_records_what_the_hook_wrote
Red: a_push_that_is_accepted_records_what_it_wrote_too
Red: a_pre_push_hook_that_rejects_the_push_is_reported_as_the_merge_path_refusing
Red: a_refusing_merge_path_stops_the_publication_and_leaves_the_work_where_it_can_be_found

diff --git a/crates/onevcs/src/publish.rs b/crates/onevcs/src/publish.rs
index a94d239..4229209 100644
--- a/crates/onevcs/src/publish.rs
+++ b/crates/onevcs/src/publish.rs
@@ -1563,33 +1563,20 @@ pub(crate) fn record_push(
     preserve_under: Option<&Path>,
 ) -> Result<()> {
     let output = pushed.output();
-    let stored = stream::store_artifact("log", output);
-    let kept = preserve_under.map(|root| merge_path::preserve_log(root, branch, output));
-    for error in [
-        stored.as_ref().err(),
-        kept.as_ref().and_then(|kept| kept.as_ref().err()),
-    ]
-    .into_iter()
-    .flatten()
-    {
-        eprintln!(
-            "onevcs: warning: the push of {branch:?} is recorded without what it wrote: {error}"
-        );
-    }
-    let artifact = stored.ok();
-    let mut payload = object(json!({
-        "branch": branch,
-        "remote": "origin",
-        "accepted": pushed.accepted(),
-        "output": output,
-    }));
-    if let Some(Ok(preserved)) = &kept {
-        payload.insert(
-            "preserved_log".to_owned(),
-            json!(preserved.display().to_string()),
-        );
+    stream.emit(
+        EventKind::Push,
+        object(json!({
+            "branch": branch,
+            "remote": "origin",
+            "accepted": pushed.accepted(),
+        })),
+    );
+    if !pushed.accepted() {
+        let _ = stream::store_artifact("log", output)?;
+        if let Some(root) = preserve_under {
+            let _ = merge_path::preserve_log(root, branch, output)?;
+        }
     }
-    stream.emit_with(EventKind::Push, payload, artifact.into_iter().collect());
     Ok(())
 }
 
