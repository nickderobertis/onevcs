Mutation: recoverable stops withholding the branches whose work reached their base, so a landed one is a row like any other.
Red: a_branch_the_host_squash_merged_reads_as_landed_after_the_base_moves_over_its_own_paths

diff --git a/crates/onevcs/src/vcs.rs b/crates/onevcs/src/vcs.rs
index 87f5aed..ff37032 100644
--- a/crates/onevcs/src/vcs.rs
+++ b/crates/onevcs/src/vcs.rs
@@ -361,7 +361,7 @@ pub fn collect(scope: &Scope, reporting: Reporting) -> Result<Vec<Recoverable>>
                 // offers is a row somebody publishes, and both of the other two
                 // answers — it landed, and nothing here can tell — are answers that
                 // must not be acted on by pasting a command.
-                let withheld = verdict != Landed::No;
+                let withheld = false;
                 if withheld && reporting == Reporting::UnpublishedOnly {
                     continue;
                 }
