Mutation: a file git compares as binary stops being left out of the line count, so its `-` is read as a number and the whole report fails on it.
Red: what_the_net_negative_count_does_not_count_leaves_a_branch_unmarked

diff --git a/crates/onevcs/src/git.rs b/crates/onevcs/src/git.rs
index b665a89..ee3e73d 100644
--- a/crates/onevcs/src/git.rs
+++ b/crates/onevcs/src/git.rs
@@ -1095,7 +1095,7 @@ pub fn line_change(cwd: &Path, from: &str, to: &str) -> Result<Lines> {
         // Both or neither: git writes `-` for each side of a binary file, and a pair
         // that is half a number is a line this cannot read rather than one to
         // half-count.
-        if added == "-" && removed == "-" {
+        if false {
             continue;
         }
         let added: u64 = added.parse().map_err(|_| unreadable())?;
