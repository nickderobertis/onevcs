Mutation: whether the lease was declined is read out of git's diagnostic prose.
Red: a_leased_push_the_host_refuses_is_read_from_what_git_reports_and_not_from_its_wording

diff --git a/crates/onevcs/src/publish.rs b/crates/onevcs/src/publish.rs
index b7193f8..810393b 100644
--- a/crates/onevcs/src/publish.rs
+++ b/crates/onevcs/src/publish.rs
@@ -960,19 +960,8 @@ fn declined_the_lease(
     replaced: &str,
     environment: &[(String, String)],
 ) -> Result<bool> {
-    if !pushed.refused_branch(&context.branch) {
-        return Ok(false);
-    }
-    Ok(
-        match git::remote_tip(&context.worktree, "origin", &context.branch, environment)? {
-            git::RemoteTip::At(tip) => tip.as_str() != replaced,
-            // The host no longer has the branch at all, which is not where this run
-            // last saw it either — the lease named a commit, and git declines it
-            // against a ref that is gone just as it does against one that moved.
-            git::RemoteTip::Absent => true,
-            git::RemoteTip::Unknown => false,
-        },
-    )
+    let _ = (context, replaced, environment);
+    Ok(pushed.output().contains("stale info"))
 }
 
 /// Push the branch, open or adopt its change request, and ask the host to land it.
