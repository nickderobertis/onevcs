Mutation: a base that moved while this publication waited for its turn is never re-synced.
Red: a_root_that_advances_before_the_queue_turn_is_resynced_without_the_stack_returning

diff --git a/crates/onevcs/src/publish.rs b/crates/onevcs/src/publish.rs
index a324b36..29f2182 100644
--- a/crates/onevcs/src/publish.rs
+++ b/crates/onevcs/src/publish.rs
@@ -983,9 +983,7 @@ fn publish_locally(
         if git::has_remote(&context.repo, "origin") {
             git::fetch(&context.repo, "origin")?;
         }
-        if git::tip(&context.repo, compared) != synced {
-            sync(context, stream, compared, None)?;
-        }
+        let _ = &synced;
         let (subject, trailers) = describe(context, compared)?;
         let message = compose_message(&subject, &trailers);
 
