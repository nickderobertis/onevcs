Mutation: a path list this process could not read is answered as a commit that changed nothing.
Red: a_stack_parent_whose_path_this_process_cannot_read_is_replayed_by_content_alone

diff --git a/crates/onevcs/src/git.rs b/crates/onevcs/src/git.rs
index 013dc80..e8a4edf 100644
--- a/crates/onevcs/src/git.rs
+++ b/crates/onevcs/src/git.rs
@@ -773,9 +773,6 @@ pub fn carries_changes(cwd: &Path, base: &str, fork: &str, commit: &str) -> Resu
         // one is answered by the same question asked without paths: a base carrying
         // this commit's whole tree carries its changes too, and that is the one
         // answer that cannot be wrong about a path nobody here could name.
-        if trees_differ(cwd, fork, commit)? {
-            return Ok(!trees_differ(cwd, base, commit)?);
-        }
         return Ok(false);
     }
     let mut args = vec!["diff", "--quiet", commit, base, "--"];
