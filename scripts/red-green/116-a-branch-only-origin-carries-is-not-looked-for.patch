Mutation: the copy origin holds stops being one of the copies a pin is continued from, so a branch no checkout here has ever seen is cut fresh over it.
Red: a_branch_pin_naming_work_that_already_exists_continues_it_rather_than_cutting_fresh
Red: a_continued_branch_opens_at_whichever_copy_carries_the_other

diff --git a/crates/onevcs/src/workspace.rs b/crates/onevcs/src/workspace.rs
index 2567306..ad5df8c 100644
--- a/crates/onevcs/src/workspace.rs
+++ b/crates/onevcs/src/workspace.rs
@@ -924,10 +924,8 @@ fn continuation(
         }
         false => None,
     };
-    let pushed = git::tip(execution, &format!("refs/remotes/origin/{branch}")).map(|tip| Carried {
-        checkout: None,
-        tip,
-    });
+    let pushed = None;
+    let _ = execution;
     Ok(match (held, pushed) {
         (Some(held), Some(pushed)) => Some(Continued::Both { held, pushed }),
         (Some(held), None) => Some(Continued::Held(held)),
