Mutation: the status report writes an optional field even when it holds nothing, so a consumer that never heard of a session is handed one that is null.
Red: the_status_report_is_the_versioned_object_its_goldens_record

diff --git a/crates/onevcs/src/status.rs b/crates/onevcs/src/status.rs
index 7c43fc6..3cc3324 100644
--- a/crates/onevcs/src/status.rs
+++ b/crates/onevcs/src/status.rs
@@ -66,7 +66,6 @@ pub struct Report {
     /// The identity the work belongs to, and the policy its rules resolve to.
     pub identity: IdentityReport,
     /// The session that holds or held the branch, when one is recorded.
-    #[serde(skip_serializing_if = "Option::is_none")]
     pub session: Option<SessionReport>,
     /// The branch itself: who has it, what it carries, and what it records.
     pub branch: BranchReport,
