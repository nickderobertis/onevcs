Mutation: the refusal stops naming the commit each copy stands on, so nothing says where the two forked apart.
Red: a_copy_amended_in_one_checkout_is_refused_naming_both_trees_and_how_they_differ
Red: copies_of_one_branch_that_have_diverged_refuse_the_landing_and_name_each_one

diff --git a/crates/onevcs/src/branch.rs b/crates/onevcs/src/branch.rs
index da895ee..97822bf 100644
--- a/crates/onevcs/src/branch.rs
+++ b/crates/onevcs/src/branch.rs
@@ -767,11 +767,10 @@ fn differ(into: &Held, into_shape: &git::Shape, from: &Held, from_shape: &git::S
 /// One copy's commit, as the facts the comparison is made of.
 fn facts_of(copy: &Held, shape: &git::Shape) -> String {
     format!(
-        "the copy in {path} stands at {tip}, on parent(s) {parents:?}, with the subject \
+        "the copy in {path} stands at {tip}, with the subject \
          {subject:?}, the tree {tree}, and the commit date {committed}",
         path = copy.checkout.display(),
         tip = copy.tip,
-        parents = shape.parents,
         subject = shape.subject,
         tree = shape.tree,
         committed = shape.committed,
