Mutation: import stops refusing a non-fast-forward, so a name a checkout holds work under is written over by whatever the source has.
Red: an_import_that_would_not_fast_forward_is_refused_naming_what_it_would_lose

diff --git a/crates/onevcs/src/import.rs b/crates/onevcs/src/import.rs
index 41cf681..a34dba6 100644
--- a/crates/onevcs/src/import.rs
+++ b/crates/onevcs/src/import.rs
@@ -205,7 +205,7 @@ fn refuse_a_rewrite(
     repo: &Path,
     branch: &str,
 ) -> Result<()> {
-    if git::is_ancestor(destination, name, scratch)? {
+    if git::is_ancestor(destination, name, scratch)? || true {
         return Ok(());
     }
     let losing = git::log_messages(destination, scratch, name)?;
