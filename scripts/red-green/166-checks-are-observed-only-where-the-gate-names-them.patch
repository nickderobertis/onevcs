Mutation: a change-direct publication consults the host's checks only where the resolved policy names them as its gate, which on a host whose rules name commands is no repository at all.
Red: a_host_that_will_not_describe_a_change_requests_checks_still_opens_one
Red: a_host_that_queues_a_direct_merge_is_reported_as_queued_rather_than_as_landed

diff --git a/crates/onevcs/src/publish.rs b/crates/onevcs/src/publish.rs
index cd7532f..efc921b 100644
--- a/crates/onevcs/src/publish.rs
+++ b/crates/onevcs/src/publish.rs
@@ -1246,7 +1246,12 @@ fn publish_as_change(
         // gate that is no identity at all: the host's required checks were observed
         // for no repository. A change-direct publication asks for the merge itself,
         // so it waits for the checks the host says block one first.
-        if context.effective == MergePolicy::ChangeDirect {
+        if matches!(
+            context.policy.gate,
+            Gate::Kind {
+                kind: GateKind::Checks
+            }
+        ) {
             await_checks(host.as_ref(), &change, stream)?;
         }
         stream.emit(
