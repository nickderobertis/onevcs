Mutation: the tip a session was cut from is never written down, so no session records a stack.
Red: a_stacked_session_records_the_tip_it_was_cut_from_and_keeps_it_through_its_life


diff --git a/crates/onevcs/src/workspace.rs b/crates/onevcs/src/workspace.rs
index bf527c9..39fff1c 100644
--- a/crates/onevcs/src/workspace.rs
+++ b/crates/onevcs/src/workspace.rs
@@ -607,13 +607,7 @@ pub fn open(registry: &Registry, request: &SessionRequest) -> Result<(Record, St
         base.to_string()
     };
     git::worktree_add(&clone, &worktree, &branch, &start)?;
-    // Read off the worktree that was just cut, which is where the commit it was cut at
-    // is by construction — asking the name it was cut from again could answer
-    // something else, or nothing.
-    let stack_tip = match &root {
-        Some(root) if **root != *base => Some(git::head_sha(&worktree)?),
-        _ => None,
-    };
+    let stack_tip = None;
 
     let record = Record {
         version: RECORD_VERSION,
