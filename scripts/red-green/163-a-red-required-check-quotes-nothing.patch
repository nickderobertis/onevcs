Mutation: a required check that concluded red is named and its log is not quoted, leaving the reason reachable only by fetching the artifact by hand.
Red: a_red_required_check_ends_an_auto_merge_publication_and_quotes_the_log

diff --git a/crates/onevcs/src/publish.rs b/crates/onevcs/src/publish.rs
index cd7532f..dfbf444 100644
--- a/crates/onevcs/src/publish.rs
+++ b/crates/onevcs/src/publish.rs
@@ -1507,14 +1507,8 @@ fn watch<T>(
 /// an excerpt naming the reason there is no excerpt would read as the check's own
 /// output.
 fn checks_failed(check: &Check, logs: &[(String, crate::event::ArtifactId)]) -> Error {
-    let said = logs
-        .iter()
-        .find(|(name, _)| name == &check.name)
-        .and_then(|(_, id)| stream::read_artifact(&id.0).ok())
-        .map(|log| excerpt(&log))
-        .filter(|log| !log.trim().is_empty())
-        .map(|log| format!(". It said:\n{}", guidance::quoted_output(&log)))
-        .unwrap_or_default();
+    let _ = logs;
+    let said = String::new();
     Error::ChecksFailed {
         reason: format!(
             "required check {:?} concluded {}{said}",
