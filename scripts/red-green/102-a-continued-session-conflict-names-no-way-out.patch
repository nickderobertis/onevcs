Mutation: the refusal a continued branch's conflict gives names neither the copy it is in nor the command that lands it.
Red: a_continued_branch_whose_base_conflicts_is_refused_naming_where_it_is_and_what_lands_it

diff --git a/crates/onevcs/src/workspace.rs b/crates/onevcs/src/workspace.rs
index 841a2ce..d492c09 100644
--- a/crates/onevcs/src/workspace.rs
+++ b/crates/onevcs/src/workspace.rs
@@ -1052,6 +1052,7 @@ fn integrate(
     opened: &Carried,
     publication: &Path,
 ) -> Result<()> {
+    let _ = (publication, guidance::command(["onevcs"]), opened.at());
     if let crate::publish::Reconciled::Settled =
         crate::publish::reconcile(worktree, integrated, branch, None)?
     {
@@ -1059,21 +1060,8 @@ fn integrate(
     }
     Err(Error::SyncConflict {
         reason: format!(
-            "{integrated} conflicts with branch {branch:?} at {tip}, which is the copy in {at} \
-             this session would continue, so opening it would leave a conflict in a worktree \
-             nobody asked to resolve. The branch is untouched. Resolve the conflict on it — check \
-             it out in {publication} and merge {integrated} into it — and open this session \
-             again, or land it as it stands with `{land}`",
+            "{integrated} conflicts with branch {branch:?} at {tip}; the session was not opened",
             tip = opened.tip,
-            at = opened.at(),
-            publication = publication.display(),
-            land = guidance::command([
-                "onevcs",
-                "publish-branch",
-                branch,
-                "--repo",
-                &publication.to_string_lossy(),
-            ]),
         ),
     })
 }
