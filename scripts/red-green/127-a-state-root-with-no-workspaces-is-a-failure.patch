Mutation: a state root nothing has published from is read as a sweep that could not run, rather than as one with nothing to do.
Red: a_state_root_nothing_has_published_from_is_a_sweep_with_nothing_to_do

diff --git a/crates/onevcs/src/sweep.rs b/crates/onevcs/src/sweep.rs
index e904725..daefd93 100644
--- a/crates/onevcs/src/sweep.rs
+++ b/crates/onevcs/src/sweep.rs
@@ -85,7 +85,6 @@ pub fn run(dry_run: bool, min_age: Duration) -> Result<Report> {
                 });
             }
         }
-        Err(e) if e.kind() == std::io::ErrorKind::NotFound => {}
         Err(e) => return Err(error::at("read the workspaces under", &root)(e)),
     }
     report
