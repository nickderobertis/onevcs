Mutation: a root that already carries the change below a stack is merged into like anything else.
Red: a_recorded_stack_that_squash_merged_is_replayed_onto_the_root_rather_than_merged
Red: a_conflict_in_a_replayed_branchs_own_work_is_refused_with_the_replay_that_lands_it
Red: a_stack_whose_paths_this_process_cannot_read_is_answered_by_content_alone
Red: a_recovery_whose_recorded_stack_already_landed_is_replayed_onto_the_root
Red: a_recoverys_replay_conflict_keeps_the_branch_and_names_the_replay
Red: a_publish_branch_whose_recorded_stack_already_landed_is_replayed_onto_the_root
Red: a_hosted_stack_whose_change_below_landed_opens_its_review_against_the_root
Red: a_hosted_stack_the_root_independently_matches_is_answered_the_same_way

diff --git a/crates/onevcs/src/publish.rs b/crates/onevcs/src/publish.rs
index 7782dcb..f4348d1 100644
--- a/crates/onevcs/src/publish.rs
+++ b/crates/onevcs/src/publish.rs
@@ -406,23 +406,8 @@ fn recorded_stack(record: &workspace::Record, publication: &Path) -> Option<Stac
 /// work is what is left either way. A change still open whose content has *not*
 /// reached the root fails the last test, and one merged as its own commits fails the
 /// second.
-pub(crate) fn root_carries_the_stack(repo: &Path, branch: &str, stack: &Stack) -> Result<bool> {
-    let root = vcs::base_ref(repo, &stack.root);
-    if !git::ref_exists(repo, &format!("refs/remotes/{root}")) && !git::branch_exists(repo, &root) {
-        return Ok(false);
-    }
-    if !git::is_ancestor(repo, &stack.tip, branch)? {
-        return Ok(false);
-    }
-    // Still in the root as the commits it was written as: merging the root brings
-    // them in the way it always has, and there is nothing to replay past.
-    if git::is_ancestor(repo, &stack.tip, &root)? {
-        return Ok(false);
-    }
-    let Some(fork) = git::merge_base(repo, &root, &stack.tip)? else {
-        return Ok(false);
-    };
-    git::carries_changes(repo, &root, &fork, &stack.tip)
+pub(crate) fn root_carries_the_stack(_repo: &Path, _branch: &str, _stack: &Stack) -> Result<bool> {
+    Ok(false)
 }
 
 impl<'a> Context<'a> {
