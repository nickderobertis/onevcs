Mutation: a stack parent the base already carries is merged into the branch like anything else.
Red: a_stack_parent_that_squash_merged_is_replayed_out_of_the_branch_rather_than_merged
Red: a_conflict_in_a_replayed_branchs_own_work_is_refused_with_the_replay_that_lands_it
Red: a_stack_parent_the_base_already_carries_is_replayed_by_the_verb_that_publishes_it
Red: a_replay_conflict_in_a_named_branchs_own_work_names_the_replay_that_resolves_it
Red: a_recovery_whose_stack_parent_already_landed_is_replayed_onto_the_base
Red: a_recoverys_replay_conflict_keeps_the_branch_and_names_the_replay


diff --git a/crates/onevcs/src/publish.rs b/crates/onevcs/src/publish.rs
index e903e73..64474ca 100644
--- a/crates/onevcs/src/publish.rs
+++ b/crates/onevcs/src/publish.rs
@@ -540,7 +540,7 @@ pub(crate) fn reconcile(
     compared: &str,
     branch: &str,
 ) -> Result<Reconciled> {
-    let (shape, integrated) = match landed_history_boundary(repo, compared, branch)? {
+    let (shape, integrated) = match None::<String> {
         Some(parent) => (
             Reconciliation::Replay {
                 from: parent.clone(),
