Mutation: a branch is located by name alone, spent copy or not.
Red: a_name_the_checkout_has_spent_does_not_answer_for_the_run_clone_that_reuses_it

diff --git a/crates/onevcs/src/branch.rs b/crates/onevcs/src/branch.rs
index 384ae51..93217aa 100644
--- a/crates/onevcs/src/branch.rs
+++ b/crates/onevcs/src/branch.rs
@@ -387,11 +387,8 @@ fn locate(
         if !git::is_repo(candidate) || !git::branch_exists(candidate, branch) {
             continue;
         }
-        let compared = crate::vcs::judged_against(candidate, base, current.as_deref());
-        if git::trees_differ(candidate, &compared, branch)? {
-            return Ok(candidate.clone());
-        }
-        spent.get_or_insert_with(|| candidate.clone());
+        let _ = &current;
+        return Ok(candidate.clone());
     }
     if let Some(candidate) = spent {
         return Ok(candidate);
