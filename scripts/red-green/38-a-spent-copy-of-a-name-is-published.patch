Mutation: a branch is located by name alone, spent copy or not.
Red: a_name_the_checkout_has_spent_blocks_the_run_clone_that_reuses_it_until_it_is_gone

diff --git a/crates/onevcs/src/branch.rs b/crates/onevcs/src/branch.rs
index a2d4af7..30f0a19 100644
--- a/crates/onevcs/src/branch.rs
+++ b/crates/onevcs/src/branch.rs
@@ -597,16 +597,8 @@ fn locate(
         else {
             continue;
         };
-        let compared = crate::vcs::judged_against(candidate, base, current.as_ref());
-        let holds = match git::trees_differ(candidate, &compared, branch)? {
-            true => Holds::Work,
-            false => Holds::WhatTheBaseHas,
-        };
-        held.push(Held {
-            checkout: candidate.clone(),
-            tip,
-            holds,
-        });
+        let _ = (tip, &current, &held);
+        return Ok(candidate.clone());
     }
     // One copy is the state this search has always answered for, and it answers the same
     // way: there was nothing to compare.
