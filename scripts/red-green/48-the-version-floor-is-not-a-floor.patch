Mutation: the oldest readable version stops being a floor, so a version 1 document is read instead of refused.
Red: a_document_declaring_a_version_this_build_does_not_read_is_refused_by_name

diff --git a/crates/onevcs-testing/src/state.rs b/crates/onevcs-testing/src/state.rs
index e5f02d1..34032bf 100644
--- a/crates/onevcs-testing/src/state.rs
+++ b/crates/onevcs-testing/src/state.rs
@@ -565,7 +565,7 @@ impl Checked for HostState {
 /// things to do about it — one is a document to re-seed, the other a build to
 /// update.
 fn readable_version(declared: u32) -> Result<()> {
-    if !(OLDEST_READABLE_VERSION..=STATE_VERSION).contains(&declared) {
+    if declared > STATE_VERSION {
         return Err(Error::Invalid {
             reason: format!(
                 "the document declares version {declared}; this build reads versions \
