Mutation: a landing that could not be written down fails the publication, which reports a merge that has already happened as one that did not.
Red: a_landing_the_checkout_will_not_take_is_a_warning_rather_than_a_failed_publication

diff --git a/crates/onevcs/src/publish.rs b/crates/onevcs/src/publish.rs
index cd7532f..9d15445 100644
--- a/crates/onevcs/src/publish.rs
+++ b/crates/onevcs/src/publish.rs
@@ -1294,7 +1294,7 @@ fn publish_as_change(
             EventKind::MergeCompleted,
             object(json!({"identity": identity, "sha": sha.0})),
         );
-        record_landing(context, &sha);
+        write_landing(context, &sha)?;
         fast_forward_publication(&context.resolution.publication, context.target.base())?;
         Ok(PublishOutcome::Merged(sha))
     })();
