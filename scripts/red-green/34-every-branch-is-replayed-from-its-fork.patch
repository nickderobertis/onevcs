Mutation: a branch is replayed from wherever it left the base, not from what the base already carries.
Red: an_unstacked_branch_takes_a_base_that_advanced_as_the_merge_it_always_was

diff --git a/crates/onevcs/src/publish.rs b/crates/onevcs/src/publish.rs
index b0e2e5b..b135335 100644
--- a/crates/onevcs/src/publish.rs
+++ b/crates/onevcs/src/publish.rs
@@ -559,18 +559,7 @@ fn landed_stack_base(repo: &Path, compared: &str, branch: &str) -> Result<Option
     if git::is_ancestor(repo, compared, branch)? || !git::trees_differ(repo, compared, branch)? {
         return Ok(None);
     }
-    let Some(fork) = git::merge_base(repo, compared, branch)? else {
-        return Ok(None);
-    };
-    let own = git::commits_since(repo, compared, branch)?;
-    // Newest first, and never the branch's own tip: replaying from there would drop
-    // the whole branch rather than the part of it the base already has.
-    for candidate in own.iter().skip(1) {
-        if git::carries_changes(repo, compared, &fork, candidate)? {
-            return Ok(Some(candidate.clone()));
-        }
-    }
-    Ok(None)
+    git::merge_base(repo, compared, branch)
 }
 
 /// Sync the branch with the current base, bounded, before anything is published.
