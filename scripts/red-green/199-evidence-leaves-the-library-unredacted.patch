Mutation: evidence is stored as it arrived, so a credential a merge path echoed leaves the library inside an artifact.
Red: a_merge_path_that_echoes_a_credential_records_only_that_it_had_one

diff --git a/crates/onevcs/src/stream.rs b/crates/onevcs/src/stream.rs
index 7390112..28982e4 100644
--- a/crates/onevcs/src/stream.rs
+++ b/crates/onevcs/src/stream.rs
@@ -371,7 +371,7 @@ pub fn store_artifact(kind: &str, contents: &str) -> Result<ArtifactRef> {
     let id = ids::artifact_id();
     let directory = home::artifacts_dir()?;
     home::ensure_dir(&directory)?;
-    let clean = redact(contents);
+    let clean = contents.to_owned();
     let path = directory.join(&id);
     std::fs::write(&path, &clean).map_err(error::at("store the artifact at", &path))?;
     Ok(ArtifactRef {
