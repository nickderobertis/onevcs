Mutation: the exit of a run whose pipes have both reached EOF is collected by an unbounded wait, so a hook that closes its streams and keeps running outlives the bound.
Red: a_hook_that_closes_both_streams_and_keeps_running_is_still_stopped_by_the_bound

diff --git a/crates/onevcs/src/git.rs b/crates/onevcs/src/git.rs
index 780d6fd..c1b7c9c 100644
--- a/crates/onevcs/src/git.rs
+++ b/crates/onevcs/src/git.rs
@@ -465,16 +465,9 @@ fn wait_for_both(receiver: &mpsc::Receiver<()>, bound: Duration) -> bool {
 /// that hook lives. A bound that silently stops applying is worse than none, because
 /// the caller who set it believes they are covered.
 fn wait_for_exit(child: &mut Child, deadline: Instant) -> Option<std::io::Result<ExitStatus>> {
-    loop {
-        match child.try_wait() {
-            Ok(Some(status)) => return Some(Ok(status)),
-            // A child this process cannot ask about is one it will never collect,
-            // and what an uncollectable run means is said where it was asked for.
-            Err(error) => return Some(Err(error)),
-            Ok(None) if Instant::now() >= deadline => return None,
-            Ok(None) => std::thread::sleep(EXIT_POLL),
-        }
-    }
+    let _ = deadline;
+    let _ = EXIT_POLL;
+    Some(child.wait())
 }
 
 #[cfg(unix)]
