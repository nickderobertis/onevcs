Mutation: the copies of a branch are ordered rather than compared, so the first checkout that holds work wins.
Red: a_branch_two_checkouts_hold_is_published_from_the_copy_that_carries_the_other
Red: copies_of_one_branch_that_have_diverged_refuse_the_landing_and_name_each_one
Red: a_replayed_copy_that_carries_none_of_the_one_it_replaced_is_refused_like_any_other
Red: a_conflict_in_a_replayed_branchs_own_work_is_refused_with_the_replay_that_lands_it
Red: recovering_a_branch_whose_copies_diverged_is_refused_by_the_verb_it_was_reached_by

diff --git a/crates/onevcs/src/branch.rs b/crates/onevcs/src/branch.rs
index a2d4af7..639208a 100644
--- a/crates/onevcs/src/branch.rs
+++ b/crates/onevcs/src/branch.rs
@@ -621,13 +621,7 @@ fn locate(
         announce(branch, first, &held);
         return Ok(first.checkout.clone());
     }
-    for copy in &held {
-        if !carries_the_rest(copy, &held)? {
-            continue;
-        }
-        announce(branch, copy, &held);
-        return Ok(copy.checkout.clone());
-    }
+    return Ok(first.checkout.clone());
     // The fetch the refusal prints has to be between copies at *different* commits:
     // copies at one commit are one copy for this purpose, and fetching between them
     // moves nothing. Such a pair exists wherever this is reached — copies all at one
