Mutation: a copy whose content the base already carries is left out of the copies a landing reports, so the answer is not about every checkout holding the branch.
Red: a_copy_the_base_already_carries_is_named_among_the_copies_it_answers_for_none_of

diff --git a/crates/onevcs/src/branch.rs b/crates/onevcs/src/branch.rs
index 164a293..e373b2f 100644
--- a/crates/onevcs/src/branch.rs
+++ b/crates/onevcs/src/branch.rs
@@ -652,7 +652,7 @@ fn carries_the_rest(copy: &Held, working: &[&Held]) -> Result<bool> {
 fn announce(branch: &str, chosen: &Held, held: &[Held]) {
     let passed_over: Vec<String> = held
         .iter()
-        .filter(|copy| copy.tip != chosen.tip)
+        .filter(|copy| copy.tip != chosen.tip && !copy.spent)
         .map(Held::describe)
         .collect();
     if passed_over.is_empty() {
