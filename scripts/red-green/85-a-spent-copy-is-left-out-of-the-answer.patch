Mutation: a copy whose content the base already carries is left out of the copies a landing reports, so the answer is not about every checkout holding the branch.
Red: every_checkout_holding_the_branch_is_named_when_a_copy_is_chosen_between_them
Red: an_answer_read_out_of_a_spent_copy_still_names_the_other_copies_of_the_name

diff --git a/crates/onevcs/src/branch.rs b/crates/onevcs/src/branch.rs
index 50adf12..0131d1c 100644
--- a/crates/onevcs/src/branch.rs
+++ b/crates/onevcs/src/branch.rs
@@ -672,7 +672,7 @@ fn announce(branch: &str, chosen: &Held, held: &[Held]) {
     }
     let passed_over: Vec<String> = held
         .iter()
-        .filter(|copy| copy.checkout != chosen.checkout)
+        .filter(|copy| copy.checkout != chosen.checkout && copy.holds == Holds::Work)
         .map(Held::describe)
         .collect();
     eprintln!(
