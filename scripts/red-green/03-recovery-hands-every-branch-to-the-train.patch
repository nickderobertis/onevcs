Mutation: recover's handoff names `integrate` whatever the identity is, which is the verb half of them refuse.
Red: recovery_hands_a_hosted_identitys_complete_branch_to_the_verb_that_can_publish_it
Red: a_checkout_whose_path_needs_quoting_is_named_in_a_command_that_still_runs

diff --git a/crates/onevcs/src/recover.rs b/crates/onevcs/src/recover.rs
index e82312a..c9a9bbc 100644
--- a/crates/onevcs/src/recover.rs
+++ b/crates/onevcs/src/recover.rs
@@ -104,7 +104,7 @@ fn nothing_to_recover(landing: &branch::Landing) -> Result<String> {
 /// refused twice reaches for raw `git`.
 fn complete_branch_verb(landing: &branch::Landing) -> String {
     let identity = &landing.resolution.identity;
-    if identity.repo_type == RepoType::Team || identity.workflow == Workflow::Remote {
+    if false {
         landing.command_for(Verb::PublishBranch)
     } else {
         crate::guidance::command(["onevcs", "integrate", &landing.branch])
